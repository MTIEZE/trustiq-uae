{
  "name": "trustiq",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @trustiq/web",
    "build": "npm run build -w @trustiq/core && npm run build -w @trustiq/ai && npm run build -w @trustiq/web",
    "preview": "npm run preview -w @trustiq/web",
    "lint": "oxlint",
    "test": "npm run test -w @trustiq/core && npm run test -w @trustiq/ai",
    "test:db": "bash scripts/db-test.sh",
    "typecheck": "npm run typecheck -w @trustiq/core && npm run typecheck -w @trustiq/ai"
  },
  "devDependencies": {
    "oxlint": "^1.69.0"
  }
}
